# ONZ-App-building-test
# Project notes

## Running

The project setup info:
- `node v15.12.0`
- `yarn version 1.22.10`

Run the project:
- `node main.js "keyword" concept-net` will call the live api.

## About this task
Utilising ConceptNet (github.com/commonsense/conceptnet5/wiki/API), create an hierarchical JSON structure with all the term’s English language parent (IsA) concepts. The parent-child hierarchy should be ordered from the most general concept to the most specific. 

## Requirements  
The platform for the app must be nodejs, typescript, or a browser.  
Compose your code for readability.
Provide your solution as either as a zip file or as a git repository (e.g. a private Bitbucket repository)

Utilising ConceptNet, create a hierarchical JSON structure with all the query term’s English language parent (IsA) concepts. The parent-child hierarchy should be ordered from the most general concept to the most specific with multiple children, as necessary.
